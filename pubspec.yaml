name: survey_house_app
description: A new Flutter application.
version: 1.0.0+1
homepage: https://github.com/Intorne/survey_house_app.git
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_calendar_carousel: ^1.3.10

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_calendar_week:
    git:
      url: https://github.com/mduccc/flutter_calendar_week
      ref: 0.1.0

  rflutter_alert:
    git: git://github.com/RatelHub/rflutter_alert.git

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
  assets:
    - imagens/
